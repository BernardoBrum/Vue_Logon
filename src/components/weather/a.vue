if (navigator.geolocation) {
          navigator.geolocation.getCurrentPosition((position) => {

            fetch()
            .then((answer) => answer.json())
            .then((data) => {}
                
        };


        if (navigator.geolocation) {
            navigator.geolocation.getCurrentPosition((position) => {

                fetch()
                .then((answer) => answer.json())
                .then((data) => {
                    
                })
            }) 
        }